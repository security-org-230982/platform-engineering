{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "simple-game.serviceAccountName" . }}
  labels:
    app.kubernetes.io/name: {{ include "simple-game.name" . }}
  annotations:
{{ toYaml .Values.serviceAccount.annotations | indent 4 }}
{{- end }}
